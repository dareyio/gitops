apiVersion: v1
kind: ConfigMap
metadata:
  name: liveclasses-config
  namespace: liveclasses
data:
  # Feature flag: "bbb" or "jitsi" - determines default provider for root path
  provider: "jitsi"
  # Enable/disable providers independently
  jitsi_enabled: "true"
  bbb_enabled: "false"
  # Default path routing based on provider
  default_path: "/jitsi"
  # Streaming domain
  streaming_domain: "streaming-stg.talentos.darey.io"
  # S3 bucket configuration
  s3_bucket_name: "darey-v2-staging-liveclasses-recordings"
  s3_region: "eu-west-2"
  # Environment
  environment: "staging"

