apiVersion: v1
kind: Secret
metadata:
  name: bbb-secrets
  namespace: liveclasses
type: Opaque
stringData:
  api-secret: "CHANGE_ME_GENERATE_SECURE_SALT"
  # For production, use External Secrets Operator to sync from AWS Secrets Manager
  # Example ExternalSecret:
  # apiVersion: external-secrets.io/v1beta1
  # kind: ExternalSecret
  # metadata:
  #   name: bbb-secrets
  #   namespace: liveclasses
  # spec:
  #   secretStoreRef:
  #     name: aws-secrets-manager
  #     kind: SecretStore
  #   target:
  #     name: bbb-secrets
  #     creationPolicy: Owner
  #   data:
  #     - secretKey: api-secret
  #       remoteRef:
  #         key: prod/liveclasses/bbb-api-secret

