apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: dareyscore
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
    - port: 9187
      targetPort: 9187
      name: metrics
