apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: postgres
  namespace: dareyscore
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: postgres
  namespaceSelector:
    matchNames:
      - dareyscore
  endpoints:
    - port: metrics
      interval: 30s
      scrapeTimeout: 10s

